source 'https://rubygems.org'

gem 'bundler'
gem 'rake'
gem 'quality'
gem 'PriorityQueue' # used until I make my own fib heap in dijstra's algorithm
gem 'algorithms' # reference implementations for tests, I swear!
gem 'rspec', '>=3.0.0.beta2'
